package com.example.erikbrowne.mvvmdemo.viewmodels

/*
object MainViewModelSpekSpecificationTest : Spek({
	describe("spek specification test") {
		val viewModel by memoized { MainViewModel(mock(), mock(), mock()) }
		val dependency by memoized { mock<Dependency>() }

		describe("spekTest") {
			lateinit var returnVal: String

			before {
				returnVal = viewModel.spekTest(dependency)
			}

			it("calls startSomething on dependency") {
				verify(dependency).startSomething()
			}

			it("returns value") {
				assertEquals("value", returnVal)
			}
		}
	}
})
*/

/*
object MainViewModelSpekGherkinTest : Spek({
	Feature("spek gherkin test") {
		val viewModel by memoized { MainViewModel(mock(), mock(), mock()) }
		val dependency by memoized { mock<Dependency>() }

		Scenario("spekTest") {
			lateinit var returnVal: String

			When("spekTest is called") {
				returnVal = viewModel.spekTest(dependency)
			}

			Then("startSomething is called on dependency") {
				verify(dependency).startSomething()
			}

			Then("it returns value") {
				assertEquals("value", returnVal)
			}
		}
	}
})
*/
